# Frontend assets

Place visual files in these folders:

- `logos/` — brand logos and marks
- `images/` — photos, illustrations and backgrounds
- `icons/` — custom icons

Use an asset in HTML like this:

```html
<img src="/assets/images/example.png" alt="Description">
```
